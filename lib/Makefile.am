## Makefile.am for DirectFB/lib

if ENABLE_ONE
ONE_DIR = One
endif

if ENABLE_VOODOO
VOODOO_DIR = voodoo
endif

if ENABLE_SAWMAN
SAWMAN_DIR = sawman
endif

if ENABLE_FUSIONSOUND
FUSIONSOUND_DIR = fusionsound
endif

SUBDIRS = direct fusion $(ONE_DIR) $(VOODOO_DIR) $(SAWMAN_DIR) $(FUSIONSOUND_DIR)

