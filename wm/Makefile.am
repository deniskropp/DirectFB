## Makefile.am for DirectFB/wm

if ENABLE_UNIQUE
UNIQUE_DIR = unique
else
UNIQUE_DIR =
endif

if ENABLE_SAWMAN
SAWMAN_DIR = sawman
else
SAWMAN_DIR =
endif

SUBDIRS = default $(UNIQUE_DIR) $(SAWMAN_DIR)
